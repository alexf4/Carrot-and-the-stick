[build]
  builder = "dockerfile"

[[services]]
name = "backend"
source = "."
dockerfile = "backend/Dockerfile"
watchPatterns = ["backend/**"]

[services.deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30

[services.variables]
PORT = "8000"
